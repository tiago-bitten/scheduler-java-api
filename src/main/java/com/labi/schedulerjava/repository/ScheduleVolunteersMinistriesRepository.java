package com.labi.schedulerjava.repository;

import com.labi.schedulerjava.domain.ScheduleVolunteersMinistries;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScheduleVolunteersMinistriesRepository extends JpaRepository<ScheduleVolunteersMinistries, Long> {
}
